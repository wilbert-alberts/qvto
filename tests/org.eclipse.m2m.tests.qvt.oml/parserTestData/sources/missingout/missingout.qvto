transformation missingout;
metamodel 'http://www.eclipse.org/emf/2002/Ecore';

mapping impl(pack: ecore::EPackage): ecore::EPackage {
    init {
        var foo := object { name := 'foo'; }
    }
    object {
        name := 'foo';
    }
}
