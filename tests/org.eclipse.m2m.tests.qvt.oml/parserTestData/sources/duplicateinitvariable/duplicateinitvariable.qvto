transformation duplicateinitvariable;
metamodel 'http://www.eclipse.org/emf/2002/Ecore';

mapping base(): ecore::EPackage {
    init {
        var foo := 'bar';
        var foo := 'abc';
    }
    object ecore::EPackage {
        name := foo;
    }
}
