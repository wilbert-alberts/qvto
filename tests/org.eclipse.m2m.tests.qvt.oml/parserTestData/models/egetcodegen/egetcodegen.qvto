transformation egetcodegen;

modeltype ECORE uses 'http://www.eclipse.org/emf/2002/Ecore';

mapping main(in p : ecore::EOperation) : ecore::EOperation {
   init {
       var f := p.eClass().getEStructuralFeature('eType');
   }   
   eType := p.eGet(f).oclAsType(ecore::EClassifier);
}
