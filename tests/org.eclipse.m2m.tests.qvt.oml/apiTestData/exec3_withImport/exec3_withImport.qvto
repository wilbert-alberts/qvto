import exec3_lib;
import org.eclipse.m2m.tests.qvt.oml.bbox.SimpleJavaLibrary;

modeltype Ecore uses 'http://www.eclipse.org/emf/2002/Ecore';

transformation exec3_withImport(out output : Ecore);

main() {
	var v := object EClass{}.map toEClass();
	dump(v.getName());
	dump(echoFromSimpleJavaLibrary("echo"));
}

